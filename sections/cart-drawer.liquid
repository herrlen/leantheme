{%- comment -%}
  Cart Drawer Section Wrapper
  Thin wrapper so the Section Rendering API can refresh the cart drawer.
  All logic lives in snippets/cart-drawer.liquid.
{%- endcomment -%}

{% render 'cart-drawer' %}

{% schema %}
{
  "name": "Cart Drawer",
  "limit": 1,
  "disabled_on": {
    "groups": ["*"]
  },
  "settings": []
}
{% endschema %}
