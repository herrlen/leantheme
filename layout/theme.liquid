<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="canonical" href="{{ canonical_url }}">

    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}

    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}
{%- liquid
      assign body_font = settings.type_body_font
      assign header_font = settings.type_header_font
      assign body_font_bold = body_font | font_modify: 'weight', 'bold'
      assign body_font_italic = body_font | font_modify: 'style', 'italic'
    %}

    <style>
      /* 1. Schriften laden (Shopify CDN = DSGVO OK, da Content Delivery Network) */
      {{ body_font | font_face: font_display: 'swap' }}
      {{ header_font | font_face: font_display: 'swap' }}
      {{ body_font_bold | font_face: font_display: 'swap' }}
      {{ body_font_italic | font_face: font_display: 'swap' }}

      /* 2. Variablen definieren */
      :root {
        --font-body-family: {{ body_font.family }}, {{ body_font.fallback_families }};
        --font-header-family: {{ header_font.family }}, {{ header_font.fallback_families }};
        
        --font-body-weight: {{ body_font.weight }};
        --font-header-weight: {{ header_font.weight }};
      }
    </style>
    {% # Inlined CSS Variables %}
    {% render 'css-variables' %}

    {% comment %}
      Font preloading:
      1. Preconnect to font CDN for faster connection
      2. Preload only the critical font variant (base weight)
      3. Additional variants load on-demand via @font-face
    {% endcomment %}
    {% unless settings.type_primary_font.system? %}
      <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>

      {% comment %} Preload the base font variant for initial page render {% endcomment %}
      {{ settings.type_primary_font | font_url | preload_tag: as: 'font', crossorigin: 'anonymous' }}
    {% endunless %}

    {% # Load and preload the critical CSS %}
    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}

    {% # Social, title, etc. %}
    {% render 'meta-tags' %}

    {{ content_for_header }}
  </head>

  <body>
    {% sections 'header-group' %}

    {{ content_for_layout }}

    {% sections 'footer-group' %}
  <script>
  // Lean Header Scroll Logic
  document.addEventListener('DOMContentLoaded', function() {
    const header = document.querySelector('.site-header');
    
    // Funktion die prüft, ob gescrollt wurde
    function handleScroll() {
      if (window.scrollY > 20) { // Ab 20px scrollen
        header.classList.add('is-scrolled');
      } else {
        header.classList.remove('is-scrolled');
      }
    }

    // Nur aktivieren, wenn der Header existiert
    if (header) {
      window.addEventListener('scroll', handleScroll);
    }
  });
</script>
    </body>
    {% section 'footer' %}
</html>
